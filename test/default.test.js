/* eslint-disable */

describe('Testing the testing harness has been setup correctly', function () {
  it('true should be true', function () {
    expect(true).toBe(true);
  });

  it('document should exist', function () {
    expect(document).toBeDefined();
  });
});
